# Favicon de L’olla d’en Xavi — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Mostrar el logotipo existente de L’olla d’en Xavi como favicon del sitio.

**Architecture:** `index.html` referenciará directamente el PNG del logotipo mediante una etiqueta estándar en `<head>`. La prueba estática verificará tanto la declaración como la existencia del recurso.

**Tech Stack:** HTML5 y pruebas Node.js con `node:test`.

## Global Constraints

- Reutilizar `img/Logo negro contorno blanco grueso l_olla_d_en_xavi.png` sin modificarlo.
- No generar imágenes ni dependencias nuevas.
- No modificar ningún elemento visible de la página.

---

### Task 1: Declarar y verificar el favicon

**Files:**
- Modify: `index.html`
- Modify: `tests/site.test.mjs`

**Interfaces:**
- Consumes: el PNG local existente del logotipo.
- Produces: una declaración HTML estándar `rel="icon"` que los navegadores pueden cargar.

- [ ] **Step 1: Write the failing test**

Añadir una prueba que lea `index.html`, compruebe una única etiqueta `rel="icon"`, valide `type="image/png"`, la ruta exacta del logotipo y la existencia del archivo.

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test tests/site.test.mjs`

Expected: FAIL porque todavía no existe la declaración del favicon.

- [ ] **Step 3: Write minimal implementation**

Añadir dentro de `<head>`:

```html
<link rel="icon" type="image/png" href="img/Logo negro contorno blanco grueso l_olla_d_en_xavi.png">
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test tests/site.test.mjs`

Expected: todas las pruebas pasan.

- [ ] **Step 5: Verify resource integrity**

Run: `node --check script.js`

Expected: salida vacía y código de estado 0.

No se incluye commit porque el directorio no es un repositorio Git.
